import React from 'react';

const TodoList = () => {
        return(
                <div>
                        <input type="text" placeholder="Tasks"
                </div>
        )
}

export default TodoList